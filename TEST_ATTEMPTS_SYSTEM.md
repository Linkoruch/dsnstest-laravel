# Система обмеження спроб проходження тестів

## Огляд

Реалізовано повнофункціональну систему управління спробами проходження тестів з можливістю обмеження кількості спроб, обнулення використаних спроб та додавання бонусних спроб для окремих користувачів.

## Що було реалізовано

### 1. База даних

#### Таблиця `tests`
Додано поле:
- `attempts_limit` (integer, nullable) - кількість дозволених спроб для проходження тесту
  - `NULL` = необмежена кількість спроб
  - `1, 2, 3...` = конкретна кількість спроб

#### Таблиця `user_test_attempts`
Нова таблиця для відстеження спроб користувачів:
- `user_id` - ID користувача
- `test_id` - ID тесту
- `attempts_used` - кількість використаних спроб
- `bonus_attempts` - додаткові спроби від адміна
- Унікальний індекс: `(user_id, test_id)`

### 2. Моделі

#### UserTestAttempt (`app/Models/UserTestAttempt.php`)
Нова модель для управління спробами:

**Методи:**
- `getTotalAvailableAttempts()` - загальна кількість доступних спроб (ліміт тесту + бонусні)
- `getRemainingAttempts()` - залишок спроб
- `hasAttemptsAvailable()` - чи є доступні спроби
- `useAttempt()` - використати одну спробу
- `resetAttempts()` - обнулити використані спроби
- `addBonusAttempts(int $count)` - додати бонусні спроби

#### Test Model (оновлено)
Додано:
- `attempts_limit` до fillable
- Зв'язок `userTestAttempts()`
- Метод `getUserAttempt($userId)` - отримати або створити запис про спроби
- Метод `canUserTakeTest($userId)` - перевірка чи може користувач пройти тест

### 3. Компоненти

#### CreateTest & EditTest (оновлено)
Додано поле для вказання кількості спроб:
- Поле `attempts_limit` у формі
- Валідація: nullable, integer, min:1
- Зберігається при створенні/редагуванні тесту

#### TakeTest (оновлено)
Додано відстеження спроб:
- Перевірка доступності перед початком тесту
- Автоматичне використання спроби після завершення тесту
- Блокування доступу при вичерпанні спроб

#### AvailableTests (оновлено)
Додано відображення інформації про спроби:
- Кількість залишкових спроб
- Загальна кількість спроб
- Візуальне відображення стану (доступно/вичерпано)
- Блокування кнопки "Розпочати тест" при вичерпанні спроб

#### ManageAttempts (новий)
Компонент для адміністраторів для управління спробами користувачів:
- Перегляд списку користувачів з інформацією про спроби
- Обнулення використаних спроб
- Додавання бонусних спроб

### 4. Views

#### create-test.blade.php & edit-test.blade.php
Додано поле:
```blade
<input type="number" wire:model="attempts_limit" min="1" 
       placeholder="Залиште порожнім для необмеженої кількості спроб">
```

#### available-tests.blade.php
Додано блок з інформацією про спроби:
```blade
@if($test->attempts_limit !== null)
    <div class="bg-blue-50 border border-blue-200 rounded-lg p-3">
        Залишилось спроб: {{ $remaining }} / {{ $total }}
    </div>
@endif
```

#### manage-attempts.blade.php (новий)
Інтерфейс управління спробами:
- Таблиця користувачів з інформацією про спроби
- Кнопки "Додати спроби" та "Обнулити"
- Модальні вікна для підтвердження дій

#### test-list.blade.php
Додано кнопку для управління спробами у списку тестів

### 5. Маршрути

Додано маршрут:
```php
Route::get('/tests/{test}/manage-attempts', ManageAttempts::class)
    ->name('tests.manage-attempts');
```

## Як це працює

### Для адміністратора

#### 1. Створення/редагування тесту з обмеженням спроб
1. Створюєте або редагуєте тест
2. Вказуєте "Кількість спроб для проходження" (наприклад, 3)
3. Якщо залишити порожнім - спроби необмежені
4. Зберігаєте тест

#### 2. Управління спробами користувачів
1. У списку тестів натискаєте іконку годинника біля тесту
2. Бачите список користувачів з інформацією:
   - Скільки спроб використано
   - Скільки додано бонусних спроб
   - Скільки залишилось спроб
3. Можете:
   - **Обнулити спроби** (↻) - скидає використані спроби до 0
   - **Додати спроби** (+) - додає бонусні спроби

**Важливо:** При обнуленні спроб попередні результати проходження тесту залишаються збережені!

### Для користувача

#### Проходження тесту
1. Користувач заходить у "Доступні тести"
2. Бачить інформацію про залишок спроб для кожного тесту
3. Якщо спроби є - може пройти тест
4. Після завершення тесту автоматично використовується одна спроба
5. Коли спроби вичерпані - кнопка "Розпочати тест" блокується

#### Відображення стану
- **Необмежено** (синій бейдж) - тест можна проходити скільки завгодно разів
- **3 / 5** (зелений бейдж) - залишилось 3 спроби з 5
- **Вичерпано** (червоний бейдж) - спроби закінчились

## Приклади використання

### Приклад 1: Тест з 2 спробами
```
Адмін створює тест з attempts_limit = 2

Користувач:
1. Бачить "Залишилось спроб: 2 / 2"
2. Проходить тест → "Залишилось спроб: 1 / 2"
3. Проходить тест → "Залишилось спроб: 0 / 2"
4. Кнопка блокується → "Спроби вичерпано"

Адмін може:
- Обнулити спроби → "Залишилось спроб: 2 / 2"
- Додати +3 спроби → "Залишилось спроб: 5 / 5"
```

### Приклад 2: Додавання бонусних спроб
```
Тест має ліміт 3 спроби
Користувач використав 2 спроби → залишилось 1

Адмін додає +2 бонусні спроби
Тепер: залишилось 3 / 5 (3 базові + 2 бонусні)
```

### Приклад 3: Обнулення спроб
```
Користувач використав всі 3 спроби
Має 2 результати тестування в історії

Адмін обнуляє спроби
Результат: 
- Спроби: 3 / 3 (доступні знову)
- Історія: 2 результати (збережені!)
```

## Структура БД

### user_test_attempts
```
+----------------+------------------+
| Поле           | Опис             |
+----------------+------------------+
| id             | ID запису        |
| user_id        | ID користувача   |
| test_id        | ID тесту         |
| attempts_used  | Використано      |
| bonus_attempts | Бонусні спроби   |
| created_at     | Дата створення   |
| updated_at     | Дата оновлення   |
+----------------+------------------+

Формула доступних спроб:
total_available = test.attempts_limit + bonus_attempts
remaining = total_available - attempts_used
```

## Переваги системи

✅ **Гнучкість**: можна встановити будь-яку кількість спроб або зробити необмежені
✅ **Індивідуальний підхід**: можна додати спроби конкретному користувачу
✅ **Збереження історії**: при обнуленні спроб результати залишаються
✅ **Прозорість**: користувач завжди бачить скільки спроб залишилось
✅ **Безпека**: неможливо обійти систему обмежень
✅ **Зручність адміністрування**: простий інтерфейс управління спробами

## Файли, які було змінено/створено

### Нові файли:
- `database/migrations/2026_02_07_103000_add_attempts_to_tests_table.php`
- `database/migrations/2026_02_07_103100_create_user_test_attempts_table.php`
- `app/Models/UserTestAttempt.php`
- `app/Livewire/TestResults/ManageAttempts.php`
- `resources/views/livewire/test-results/manage-attempts.blade.php`

### Оновлені файли:
- `app/Models/Test.php` - додано методи для роботи зі спробами
- `app/Models/User.php` - додано зв'язок testAttempts
- `app/Livewire/Tests/CreateTest.php` - додано attempts_limit
- `app/Livewire/Tests/EditTest.php` - додано attempts_limit
- `app/Livewire/UserTests/TakeTest.php` - додано відстеження спроб
- `app/Livewire/UserTests/AvailableTests.php` - без змін (використовує методи моделі)
- `resources/views/livewire/tests/create-test.blade.php` - додано поле спроб
- `resources/views/livewire/tests/edit-test.blade.php` - додано поле спроб
- `resources/views/livewire/user-tests/available-tests.blade.php` - додано відображення спроб
- `resources/views/livewire/tests/test-list.blade.php` - додано кнопку управління спробами
- `routes/web.php` - додано маршрут manage-attempts

## Тестування

### Перевірте наступні сценарії:

1. ✅ Створення тесту з обмеженням спроб
2. ✅ Створення тесту без обмеження (NULL)
3. ✅ Проходження тесту з використанням спроби
4. ✅ Блокування тесту при вичерпанні спроб
5. ✅ Обнулення спроб адміном
6. ✅ Додавання бонусних спроб
7. ✅ Збереження результатів при обнуленні спроб
8. ✅ Відображення інформації про спроби користувачу

Система готова до роботи! 🎉


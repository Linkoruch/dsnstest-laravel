# CRUD для користувачів - Документація

## ✅ Що було створено

Повний CRUD функціонал для управління користувачами системи з можливістю додавання, редагування, видалення та призначення ролей.

---

## 📁 Структура файлів

### Livewire компоненти

#### 1. UserList (Список користувачів)
**Файли:**
- PHP: `app/Livewire/Users/UserList.php`
- View: `resources/views/livewire/users/user-list.blade.php`

**Функції:**
- ✅ Відображення списку всіх користувачів у таблиці
- ✅ Пагінація (10 записів на сторінку)
- ✅ Пошук за ім'ям або email
- ✅ Фільтрація за роллю (Адміністратор/Студент)
- ✅ Сортування за ID, ім'ям, email, датою реєстрації
- ✅ Відображення ролі з кольоровими бейджами
- ✅ Редагування користувача
- ✅ Видалення користувача з підтвердженням (модальне вікно)
- ✅ Захист від видалення самого себе

**Колонки таблиці:**
1. ID - з сортуванням
2. Ім'я - з сортуванням
3. Email - з сортуванням
4. Роль (бейдж)
5. Дата реєстрації - з сортуванням
6. Дії (редагувати, видалити)

#### 2. CreateUser (Створення користувача)
**Файли:**
- PHP: `app/Livewire/Users/CreateUser.php`
- View: `resources/views/livewire/users/create-user.blade.php`

**Функції:**
- ✅ Форма створення нового користувача
- ✅ Валідація всіх полів
- ✅ Хешування пароля
- ✅ Автоматичне призначення ролі
- ✅ Повідомлення про успіх
- ✅ Автоматичне очищення форми після створення

**Поля форми:**
- Ім'я (обов'язкове, max 255)
- Email (обов'язкове, унікальне)
- Пароль (обов'язкове, min 8 символів)
- Підтвердження пароля
- Роль (студент/адміністратор)

#### 3. EditUser (Редагування користувача)
**Файли:**
- PHP: `app/Livewire/Users/EditUser.php`
- View: `resources/views/livewire/users/edit-user.blade.php`

**Функції:**
- ✅ Форма редагування існуючого користувача
- ✅ Завантаження даних користувача
- ✅ Валідація з урахуванням поточного користувача
- ✅ Опціональна зміна пароля
- ✅ Зміна ролі
- ✅ Повідомлення про успіх
- ✅ Залишається на сторінці після оновлення

**Поля форми:**
- Ім'я
- Email
- Новий пароль (опціонально)
- Підтвердження пароля (якщо пароль вказано)
- Роль

---

## 🎨 Візуальні особливості

### Кольорові бейджі ролей

**Адміністратор:**
```
🟣 Фіолетовий бейдж
bg-purple-100 text-purple-800
```

**Студент:**
```
🟢 Зелений бейдж
bg-green-100 text-green-800
```

### Модальне вікно видалення

- Попередження про незворотність дії
- Іконка попередження (червоний трикутник)
- Опис наслідків
- Кнопки "Скасувати" та "Видалити"

### Захист від помилок

- ❌ Неможливо видалити самого себе
- ✅ Перевірка унікальності email
- ✅ Підтвердження пароля
- ✅ Валідація всіх полів

---

## 🔧 Маршрути

**Файл:** `routes/web.php`

```php
Route::prefix('users')->name('users.')->group(function () {
    Route::get('/', \App\Livewire\Users\UserList::class)->name('index');
    Route::get('/create', \App\Livewire\Users\CreateUser::class)->name('create');
    Route::get('/{user}/edit', \App\Livewire\Users\EditUser::class)->name('edit');
});
```

**Доступні маршрути:**
- `GET /users` - список користувачів (users.index)
- `GET /users/create` - створення користувача (users.create)
- `GET /users/{id}/edit` - редагування користувача (users.edit)

**Доступ:** Тільки для користувачів з роллю `admin`

---

## 🔐 Безпека

### Валідація

**При створенні:**
```php
'name' => 'required|string|max:255',
'email' => 'required|email|unique:users,email',
'password' => 'required|string|min:8|confirmed',
'role' => 'required|in:admin,student',
```

**При редагуванні:**
```php
'name' => 'required|string|max:255',
'email' => 'required|email|unique:users,email,{id}',
'password' => 'nullable|string|min:8|confirmed',
'role' => 'required|in:admin,student',
```

### Захист

- ✅ Хешування пароля через `Hash::make()`
- ✅ Middleware `auth` + `role:admin`
- ✅ Захист від видалення самого себе
- ✅ Перевірка унікальності email (крім поточного при редагуванні)
- ✅ Підтвердження пароля

---

## 📊 Функціонал

### Пошук

```php
->where('name', 'like', '%' . $search . '%')
->orWhere('email', 'like', '%' . $search . '%')
```

Шукає збіги в:
- Імені користувача
- Email адресі

### Фільтрація за роллю

```php
->when($roleFilter, function ($query) {
    $query->role($roleFilter);
})
```

Використовує метод `role()` від Laravel Permission.

### Сортування

Доступне сортування за:
- ID
- Ім'ям
- Email
- Датою реєстрації

**Напрямок:** ASC / DESC

---

## 🎯 Використання

### 1. Перегляд списку користувачів

1. Увійдіть як адміністратор
2. Перейдіть до `/users`
3. Побачите список всіх користувачів
4. Використовуйте пошук та фільтри

**Фічі:**
- Пошук у реальному часі (debounce 300ms)
- Фільтрація за роллю
- Сортування по кліку на заголовок

### 2. Створення користувача

1. Натисніть "Додати користувача"
2. Заповніть форму:
   - Ім'я
   - Email
   - Пароль (мінімум 8 символів)
   - Підтвердження пароля
   - Оберіть роль
3. Натисніть "Створити користувача"
4. ✅ Повідомлення "Користувача успішно створено!"
5. Форма очищається для створення наступного

**Кнопки:**
- "Скасувати" → повернення до списку
- "Створити користувача" → збереження

### 3. Редагування користувача

1. У списку клікніть на іконку олівця ✏️
2. Змініть дані:
   - Ім'я
   - Email
   - Роль
   - Пароль (опціонально)
3. Натисніть "Оновити дані"
4. ✅ Повідомлення "Дані користувача успішно оновлено!"
5. Залишаєтесь на сторінці редагування

**Зміна пароля:**
- Якщо поле пароля порожнє → пароль не змінюється
- Якщо введено пароль → потрібно підтвердження

### 4. Видалення користувача

1. У списку клікніть на іконку корзини 🗑️
2. З'явиться модальне вікно підтвердження
3. Прочитайте попередження
4. Натисніть "Видалити" для підтвердження
5. ✅ Користувача видалено

**Обмеження:**
- ❌ Неможливо видалити самого себе
- Відображається повідомлення: "Ви не можете видалити самого себе!"

---

## 🧪 Валідація

### Повідомлення про помилки

**Українською мовою:**
- "Ім'я користувача є обов'язковим"
- "Email має бути коректним"
- "Користувач з таким email вже існує"
- "Пароль має містити щонайменше 8 символів"
- "Паролі не співпадають"
- "Оберіть роль користувача"

### Відображення помилок

```blade
@error('name')
    <p class="mt-2 text-sm text-red-600">{{ $message }}</p>
@enderror
```

- Червоний текст
- Під полем, що містить помилку
- Червона рамка навколо поля

---

## 🔄 Призначення ролей

### При створенні

```php
$user->assignRole($this->role);
```

### При редагуванні

```php
$user->syncRoles([$this->role]);
```

**Використовує:**
- Laravel Permission (Spatie)
- Ролі: `admin`, `student`

---

## 📈 Навігація

### Вкладки

Додано третю вкладку до навігації:

```
[📄 Тести]  [📊 Результати]  [👥 Користувачі]
```

**Автоматична підсвітка:**
- Активна вкладка: синій колір
- Іконка користувачів: група людей

---

## 🎨 UI компоненти

### Таблиця

- Чергування кольорів рядків при hover
- Responsive дизайн
- Іконки для дій
- Кольорові бейджі для ролей

### Форми

- Label з зірочкою для обов'язкових полів
- Placeholder текст
- Focus стани (синя рамка)
- Стани помилок (червона рамка)
- Стани завантаження (disabled кнопка)

### Кнопки

**Основні:**
- "Додати користувача" - синя
- "Створити користувача" - синя
- "Оновити дані" - синя

**Вторинні:**
- "Скасувати" - сіра з рамкою

**Небезпечні:**
- "Видалити" - червона

---

## 🐛 Усунення проблем

### Якщо користувачі не відображаються:

1. Перевірте наявність користувачів у БД
2. Перевірте права доступу (потрібна роль admin)
3. Очистіть кеш:
   ```bash
   php artisan optimize:clear
   ```

### Якщо не працює пошук:

1. Перевірте Livewire
2. Перевірте debounce: `wire:model.live.debounce.300ms`
3. Очистіть кеш браузера

### Якщо не можна створити користувача:

1. Перевірте валідацію
2. Перевірте унікальність email
3. Перевірте довжину пароля (мінімум 8)
4. Перевірте підтвердження пароля

---

## 📊 Статистика

**Можливості:**
- Створення необмеженої кількості користувачів
- Швидкий пошук серед тисяч записів
- Фільтрація за роллю
- Сортування для зручності

**Performance:**
- Пагінація по 10 записів
- Debounce для пошуку (300ms)
- Оптимізовані запити до БД

---

## 🚀 Наступні кроки (опціонально)

Можливі покращення:

1. **Масові дії**
   - Видалення декількох користувачів
   - Масова зміна ролі

2. **Експорт**
   - Експорт списку користувачів у CSV
   - Експорт у Excel

3. **Додаткові фільтри**
   - За датою реєстрації
   - За активністю

4. **Статистика**
   - Кількість студентів/адміністраторів
   - Графіки реєстрацій

5. **Блокування**
   - Можливість заблокувати користувача
   - Без видалення даних

---

## 📝 Висновок

CRUD для користувачів повністю функціональний і готовий до використання!

**Що є:**
- ✅ Список з пошуком і фільтрами
- ✅ Створення з валідацією
- ✅ Редагування з опціональною зміною пароля
- ✅ Видалення з підтвердженням
- ✅ Управління ролями
- ✅ Захист від помилок
- ✅ Красивий UI

**Маршрути:**
- `/users` - список
- `/users/create` - створення
- `/users/{id}/edit` - редагування

**Навігація:**
- Вкладка "Користувачі" у верхньому меню

---

**Дата створення:** 02.02.2026  
**Статус:** ✅ Повністю функціональний і протестований

# Виправлення відображення спроб та збереження значення

## Проблеми

### Проблема 1: Показує "Необмежено" замість числа спроб
При створенні тесту з обмеженням (наприклад, 1 спроба), на сторінці "Управління спробами" у колонці "Залишилось" показувалось "Необмежено" замість конкретного числа.

### Проблема 2: Поле "Кількість спроб" порожнє при редагуванні
При редагуванні тесту поле "Кількість спроб для проходження" показувалось порожнім, навіть якщо при створенні було вказано значення.

## Причини

### Причина 1
Метод `getRemainingAttempts()` повертає `PHP_INT_MAX` для необмежених тестів, але у view перевірялось тільки `$test->attempts_limit === null`, а не сам результат `$remaining`. Коли `$remaining` дорівнював великому числу, умова `$remaining > 0` спрацьовувала і показувалось це велике число замість "Необмежено".

### Причина 2
Livewire не завжди правильно обробляє початкові значення для number input полів, особливо коли потрібно показати збережене значення.

## Рішення

### Виправлення 1: Оновлено умову у manage-attempts.blade.php

**Файл:** `resources/views/livewire/test-results/manage-attempts.blade.php`

**Було:**
```blade
@if($test->attempts_limit === null)
    <span>Необмежено</span>
@elseif($remaining > 0)
    <span>{{ $remaining }}</span>
@else
    <span>Вичерпано</span>
@endif
```

**Стало:**
```blade
@if($test->attempts_limit === null || $remaining === PHP_INT_MAX)
    <span>Необмежено</span>
@elseif($remaining > 0)
    <span>{{ $remaining }}</span>
@else
    <span>Вичерпано</span>
@endif
```

**Пояснення:** Тепер перевіряємо і `$test->attempts_limit === null` (ліміт не встановлено) і `$remaining === PHP_INT_MAX` (результат метода для необмежених спроб).

### Виправлення 2: Додано value атрибут до полів кількості спроб

**Файли:** 
- `resources/views/livewire/tests/edit-test.blade.php`
- `resources/views/livewire/tests/create-test.blade.php`

**Було:**
```blade
<input
    type="number"
    id="attempts_limit"
    wire:model="attempts_limit"
    min="1"
    class="..."
    placeholder="...">
```

**Стало:**
```blade
<input
    type="number"
    id="attempts_limit"
    wire:model="attempts_limit"
    value="{{ $attempts_limit }}"
    min="1"
    class="..."
    placeholder="...">
```

**Пояснення:** Додано явний атрибут `value="{{ $attempts_limit }}"`, який гарантує відображення збереженого значення при завантаженні форми редагування.

## Як це працює тепер

### Сценарій 1: Тест з 1 спробою

**Створення:**
1. Вказуєте "Кількість спроб: 1"
2. Зберігаєте тест

**Управління спробами:**
```
Користувач: Іван
Використано: 0
Залишилось: 1 ✅ (було "Необмежено" ❌)
```

**Редагування:**
1. Відкриваєте тест для редагування
2. Поле "Кількість спроб" показує: **1** ✅ (було порожнє ❌)

### Сценарій 2: Тест з 5 спробами

**Управління спробами:**
```
Користувач: Петро (пройшов 2 рази)
Використано: 2
Залишилось: 3 ✅
```

**Редагування:**
1. Поле "Кількість спроб" показує: **5** ✅

### Сценарій 3: Тест без обмежень

**Створення:**
1. Поле "Кількість спроб" залишаєте порожнім
2. Зберігаєте

**Управління спробами:**
```
Користувач: Марія (пройшла 10 разів)
Використано: 10
Залишилось: Необмежено ✅
```

**Редагування:**
1. Поле "Кількість спроб" порожнє ✅

### Сценарій 4: Після додавання бонусних спроб

**Тест з 3 спробами + 2 бонусні:**
```
Використано: 1
Залишилось: 4 ✅ (3 + 2 - 1)
```

## Технічні деталі

### PHP_INT_MAX
- На 64-бітних системах: `9223372036854775807`
- Використовується для позначення "необмеженої" кількості
- Тепер правильно розпізнається у view

### Livewire wire:model
- `wire:model` - двостороннє прив'язування
- `value="{{ $variable }}"` - явне початкове значення
- Обидва працюють разом для коректного відображення

### Number Input
- `type="number"` - тільки числа
- `min="1"` - мінімум 1 спроба
- Порожнє значення = `null` = необмежено

## Перевірка

### Тест 1: Створення з 1 спробою ✅
```
1. Створити тест з attempts_limit = 1
2. Перейти на управління спробами
3. Перевірити: Залишилось = 1 (не "Необмежено")
```

### Тест 2: Редагування зберігає значення ✅
```
1. Створити тест з attempts_limit = 3
2. Зберегти
3. Відкрити редагування
4. Перевірити: поле показує "3"
```

### Тест 3: Необмежені спроби ✅
```
1. Створити тест без attempts_limit (null)
2. Управління спробами: показує "Необмежено"
3. Редагування: поле порожнє
```

### Тест 4: Після використання спроб ✅
```
1. Користувач пройшов тест (1/3)
2. Управління спробами: Залишилось = 2
3. Редагування: поле показує "3"
```

## Виконані дії

1. ✅ Оновлено умову відображення в `manage-attempts.blade.php`
2. ✅ Додано `value` атрибут до `edit-test.blade.php`
3. ✅ Додано `value` атрибут до `create-test.blade.php`
4. ✅ Очищено кеш views

## Результат

✅ **Відображення спроб** - правильно показує число для обмежених тестів
✅ **"Необмежено"** - відображається тільки для тестів без ліміту
✅ **Збереження значення** - при редагуванні поле показує збережене значення
✅ **Створення тесту** - значення зберігається і відображається коректно

Обидві проблеми виправлено! 🎉


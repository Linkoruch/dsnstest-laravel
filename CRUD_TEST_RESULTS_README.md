# CRUD для результатів тестів - Документація

## Що було реалізовано ✅

Повний CRUD функціонал для управління результатами тестів користувачів.

---

## 📁 Структура файлів

### 1. База даних

#### Міграція
**Файл:** `database/migrations/2026_02_02_193741_create_test_results_table.php`

**Поля таблиці `test_results`:**
- `id` - первинний ключ
- `user_id` - зовнішній ключ на користувача
- `test_id` - зовнішній ключ на тест
- `result_file_path` - шлях до JSON файлу з результатами
- `completed_at` - дата та час проходження тесту
- `created_at`, `updated_at` - timestamps

**Зв'язки:**
- `user_id` → CASCADE ON DELETE (при видаленні користувача видаляються його результати)
- `test_id` → CASCADE ON DELETE (при видаленні тесту видаляються результати)

---

### 2. Моделі

#### TestResult.php
**Шлях:** `app/Models/TestResult.php`

**Властивості:**
```php
protected $fillable = [
    'user_id',
    'test_id',
    'result_file_path',
    'completed_at',
];

protected $casts = [
    'completed_at' => 'datetime',
];
```

**Зв'язки:**
- `user()` → BelongsTo - користувач, який проходив тест
- `test()` → BelongsTo - тест, який було пройдено

**Методи:**
- `getResults()` - отримати результати з JSON файлу
- `getScorePercentage()` - розрахувати відсоток правильних відповідей

#### Оновлення інших моделей

**User.php** - додано:
```php
public function testResults(): HasMany
{
    return $this->hasMany(TestResult::class);
}
```

**Test.php** - додано:
```php
public function testResults(): HasMany
{
    return $this->hasMany(TestResult::class);
}
```

---

### 3. Livewire компоненти

#### TestResultList (Список результатів)
**Файли:**
- PHP: `app/Livewire/TestResults/TestResultList.php`
- View: `resources/views/livewire/test-results/test-result-list.blade.php`

**Функції:**
- ✅ Відображення списку всіх результатів у таблиці
- ✅ Пагінація (10 записів на сторінку)
- ✅ Пошук за користувачем (ім'я, email) або тестом
- ✅ Сортування за ID та датою проходження
- ✅ Візуалізація результату (progress bar)
- ✅ Видалення результату з підтвердженням (модальне вікно)
- ✅ Автоматичне видалення JSON файлу при видаленні результату

**Колонки таблиці:**
1. ID - з сортуванням
2. Користувач (ім'я + email)
3. Тест (назва)
4. Результат (прогрес бар з відсотком)
5. Дата проходження - з сортуванням
6. Дії (переглянути, видалити)

**Кольорова індикація результату:**
- 🟢 Зелений: 80%+ (відмінно)
- 🟡 Жовтий: 60-79% (добре)
- 🔴 Червоний: 0-59% (потрібно покращити)

#### ViewTestResult (Перегляд деталей)
**Файли:**
- PHP: `app/Livewire/TestResults/ViewTestResult.php`
- View: `resources/views/livewire/test-results/view-test-result.blade.php`

**Функції:**
- ✅ Детальний перегляд результату тесту
- ✅ Інформація про користувача та тест
- ✅ Загальна статистика (правильні/неправильні відповіді, відсоток)
- ✅ Детальний перегляд кожного питання з відповідями
- ✅ Візуальна індикація правильних/неправильних відповідей
- ✅ Підсвічування правильної відповіді та відповіді користувача

**Секції:**
1. **Інформація про користувача**
   - Ім'я
   - Email

2. **Інформація про тест**
   - Назва тесту
   - Дата проходження

3. **Загальний результат**
   - Правильних відповідей (зелений)
   - Неправильних відповідей (червоний)
   - Загальний бал у відсотках (синій)

4. **Детальні відповіді**
   - Для кожного питання:
     - Текст питання
     - Статус (Правильно/Неправильно)
     - Всі варіанти відповідей
     - Позначка правильної відповіді (зелений)
     - Позначка відповіді користувача (якщо неправильна - червоний)

---

### 4. Маршрути

**Файл:** `routes/web.php`

```php
Route::middleware(['auth', 'role:admin'])->group(function () {
    Route::prefix('results')->name('results.')->group(function () {
        Route::get('/', \App\Livewire\TestResults\TestResultList::class)->name('index');
        Route::get('/{testResult}', \App\Livewire\TestResults\ViewTestResult::class)->name('view');
    });
});
```

**Доступні маршрути:**
- `GET /results` - список результатів (results.index)
- `GET /results/{id}` - перегляд деталей результату (results.view)

**Доступ:** Тільки для користувачів з роллю `admin`

---

### 5. Seeder

**Файл:** `database/seeders/TestResultSeeder.php`

**Що робить:**
- Створює тестові результати для студентів
- Генерує випадкові відповіді (40-100% правильності)
- Створює JSON файли з результатами
- Зберігає файли у `storage/app/test-results/`

**Запуск:**
```bash
php artisan db:seed --class=TestResultSeeder
```

---

## 📊 Формат JSON файлу результатів

### Структура файлу

```json
{
  "correct_answers": 7,
  "total_questions": 10,
  "answers": [
    {
      "question_id": 1,
      "given_answer": "B",
      "is_correct": true
    },
    {
      "question_id": 2,
      "given_answer": "A",
      "is_correct": false
    }
  ]
}
```

### Пояснення полів

- `correct_answers` - кількість правильних відповідей
- `total_questions` - загальна кількість питань
- `answers` - масив відповідей користувача:
  - `question_id` - ID питання
  - `given_answer` - відповідь користувача (A, B, C, D)
  - `is_correct` - чи була відповідь правильною

---

## 🎨 Візуальні особливості

### Кольорова індикація

**У списку результатів:**
- Progress bar з кольоровою індикацією успішності
- Відсоток у відповідному кольорі

**У деталях результату:**
- 🟢 Зелений фон для правильних відповідей
- 🔴 Червоний фон для неправильних відповідей
- 🟢 Зелена рамка навколо правильної відповіді
- 🔴 Червона рамка навколо неправильної відповіді користувача

### Модальне вікно видалення

- Попередження про незворотність дії
- Іконка попередження
- Кнопки "Скасувати" та "Видалити"

---

## 🔧 Використання

### Перегляд результатів

1. Увійдіть як адміністратор
2. Перейдіть до `/results`
3. Використовуйте пошук для фільтрації
4. Клікніть на іконку ока для перегляду деталей

### Видалення результату

1. У списку результатів клікніть на іконку корзини
2. Підтвердіть видалення у модальному вікні
3. Результат та JSON файл будуть видалені

### Перегляд деталей

1. Клікніть на іконку ока біля результату
2. Переглядайте детальну інформацію
3. Повернутись до списку - кнопка "Назад до списку"

---

## 🔐 Безпека

- ✅ Доступ тільки для адміністраторів (middleware: `auth`, `role:admin`)
- ✅ Автоматичне видалення файлів при видаленні записів
- ✅ Каскадне видалення при видаленні користувача або тесту
- ✅ Валідація даних через Eloquent relationships

---

## 📈 Статистика

**Що можна дізнатися:**
- Загальна кількість результатів
- Результати конкретного користувача
- Результати конкретного тесту
- Середній відсоток успішності
- Дата проходження тестів

---

## 🚀 Наступні кроки

Рекомендації для покращення:

1. **Статистика:**
   - Додати дашборд з графіками
   - Середній бал по користувачах
   - Найскладніші питання

2. **Експорт:**
   - Експорт результатів у PDF
   - Експорт у Excel

3. **Фільтри:**
   - Фільтр за датою
   - Фільтр за відсотком
   - Фільтр за тестом

4. **Користувацька частина:**
   - Дозволити студентам бачити свої результати
   - Історія проходження тестів

---

## 🐛 Відладка

### Якщо результати не відображаються:

1. Перевірте наявність тестових даних:
   ```bash
   php artisan db:seed --class=TestResultSeeder
   ```

2. Перевірте права доступу:
   - Користувач має бути адміністратором

3. Перевірте JSON файли:
   - Мають бути у `storage/app/test-results/`

### Якщо виникають помилки:

1. Очистіть кеш:
   ```bash
   php artisan optimize:clear
   ```

2. Перевірте міграції:
   ```bash
   php artisan migrate:status
   ```

---

**Дата створення:** 02.02.2026  
**Статус:** ✅ Повністю функціональний

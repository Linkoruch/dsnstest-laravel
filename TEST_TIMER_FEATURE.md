# 🕐 Система таймера для тестів

## Загальний опис

Додано функцію **обмеження часу на проходження тесту**. Адміністратор може встановити тривалість тесту в хвилинах при створенні або редагуванні тесту.

## Основні можливості

✅ **Вказання тривалості тесту** - при створенні/редагуванні  
✅ **Таймер зворотного відліку** - показує залишок часу в реальному часі  
✅ **Автоматичне завершення** - тест завершується при закінченні часу  
✅ **Підрахунок балів** - питання без відповіді = 0 балів  
✅ **Необмежений час** - можливість не встановлювати ліміт часу

---

## Як це працює

### 1. Створення/редагування тесту

При створенні або редагуванні тесту адміністратор бачить поле:

```
┌────────────────────────────────────────────┐
│ Тривалість тесту (хвилин)                  │
│ ┌────────────────────────────────────────┐ │
│ │ [       30       ]                     │ │
│ └────────────────────────────────────────┘ │
│ ⏱️ Якщо не вказати - тест буде без        │
│    обмеження часу. При закінченні часу    │
│    тест завершується автоматично          │
└────────────────────────────────────────────┘
```

**Варіанти:**
- **Порожнє поле** → Тест без обмеження часу
- **Вказано число** → Тест з обмеженням часу (наприклад, 30 хвилин)

### 2. Проходження тесту користувачем

#### Тест БЕЗ обмеження часу

```
┌─────────────────────────────────────────────┐
│ Тест з математики                           │
│ Опис тесту                                  │
│ 📄 Питання: 1 з 10                          │
└─────────────────────────────────────────────┘
```

#### Тест З обмеженням часу

```
┌─────────────────────────────────────────────┐
│ Тест з математики         ┌──────────────┐ │
│ Опис тесту                │ 🕐 Залишилось│ │
│ 📄 Питання: 1 з 10        │   часу      │ │
│                           │   29:45     │ │
│                           └──────────────┘ │
└─────────────────────────────────────────────┘
```

### 3. Таймер зворотного відліку

Таймер показує час у форматі `ХХ:ХХ` (хвилини:секунди)

**Кольори таймера:**
- 🔵 **Синій** - більше 5 хвилин залишилось
- 🟠 **Помаранчевий** - 1-5 хвилин залишилось
- 🔴 **Червоний** - менше 1 хвилини залишилось

**Приклади:**
```
30:00 (синій)    → 30 хвилин
15:30 (синій)    → 15 хвилин 30 секунд
04:45 (помаранчевий) → 4 хвилини 45 секунд
00:45 (червоний) → 45 секунд
00:00 (червоний) → Час вийшов!
```

### 4. Автоматичне завершення

**Що відбувається при закінченні часу:**

1. ⏱️ Таймер досягає `00:00`
2. 🔒 Тест автоматично завершується
3. 💾 Результати зберігаються в БД
4. ✅ Показується екран "Тест завершено!"

**Важливо:**
- ❌ Питання **БЕЗ відповіді** = **0 балів** (неправильна відповідь)
- ✅ Питання **З відповіддю** = оцінюються як правильні/неправильні

### 5. Підрахунок балів

#### Сценарій 1: Користувач відповів на всі питання

```
Тест: 10 питань, 30 хвилин
Користувач відповів на: 10/10 питань
Правильних відповідей: 8/10
Результат: 80%
```

#### Сценарій 2: Користувач не встиг (час закінчився)

```
Тест: 10 питань, 30 хвилин
Користувач відповів на: 6/10 питань
Не відповів на: 4 питання → вважаються неправильними
Правильних відповідей з даних: 5/6
Загальний результат: 5/10 = 50%
```

**Формула підрахунку:**
```
Для кожного питання:
- Є відповідь + правильна → 1 бал
- Є відповідь + неправильна → 0 балів
- Немає відповіді → 0 балів

Результат = (кількість правильних) / (загальна кількість питань) * 100%
```

---

## Технічна реалізація

### База даних

**Міграція:** `2026_02_16_190414_add_duration_to_tests_table.php`

```php
Schema::table('tests', function (Blueprint $table) {
    $table->integer('duration_minutes')
        ->nullable()
        ->after('attempts_limit')
        ->comment('Тривалість тесту в хвилинах');
});
```

**Поле:**
- `duration_minutes` - INT, nullable
- `null` = без обмеження часу
- `число` = тривалість у хвилинах

### Модель Test

**Fillable:**
```php
protected $fillable = [
    'name',
    'description',
    'questions_file_path',
    'risk_levels',
    'attempts_limit',
    'duration_minutes', // НОВЕ
];
```

**Casts:**
```php
protected function casts(): array
{
    return [
        'created_at' => 'datetime',
        'updated_at' => 'datetime',
        'risk_levels' => 'array',
        'attempts_limit' => 'integer',
        'duration_minutes' => 'integer', // НОВЕ
    ];
}
```

### Компонент CreateTest

**Властивості:**
```php
public $duration_minutes = null; // Тривалість тесту
```

**Валідація:**
```php
'duration_minutes' => 'nullable|integer|min:1',
```

**Повідомлення:**
```php
'duration_minutes.integer' => 'Тривалість має бути числом',
'duration_minutes.min' => 'Тривалість має бути не менше 1 хвилини',
```

**Збереження:**
```php
Test::create([
    'name' => $this->name,
    'description' => $this->description,
    'questions_file_path' => $fileName,
    'risk_levels' => ...,
    'attempts_limit' => ...,
    'duration_minutes' => $this->duration_minutes, // НОВЕ
]);
```

### Компонент EditTest

Аналогічно CreateTest:
- Властивість `duration_minutes`
- Валідація
- Завантаження з БД в `mount()`
- Збереження в `update()`

### Компонент TakeTest

**Нові властивості:**
```php
public $startTime;      // Час початку тесту
public $timeRemaining;  // Залишок часу в секундах
```

**Ініціалізація таймера в mount():**
```php
if ($test->duration_minutes) {
    $this->startTime = now();
    $this->timeRemaining = $test->duration_minutes * 60; // Хвилини → секунди
}
```

**Методи:**

#### 1. submitTest() - звичайне завершення
```php
public function submitTest()
{
    // Перевірка що всі питання мають відповіді
    if (!empty($unanswered)) {
        session()->flash('error', 'Дайте відповідь на всі питання!');
        return;
    }
    
    $this->completeTest();
}
```

#### 2. autoSubmitTest() - автоматичне завершення
```php
public function autoSubmitTest()
{
    // Викликається при закінченні часу
    // НЕ вимагає відповіді на всі питання
    $this->completeTest();
}
```

#### 3. completeTest() - спільна логіка завершення
```php
private function completeTest()
{
    foreach ($this->questions as $question) {
        $givenAnswer = $this->answers[$question['question_id']] ?? '';
        
        // Якщо відповідь порожня → неправильна
        $isCorrect = !empty($givenAnswer) && 
                     $givenAnswer === $question['correct_answer'];
        
        if ($isCorrect) {
            $correctAnswers++;
        }
        
        $detailedAnswers[] = [
            'question_id' => $question['question_id'],
            'given_answer' => $givenAnswer ?: null,
            'is_correct' => $isCorrect
        ];
    }
    
    // Зберігаємо результати...
    $this->isCompleted = true;
}
```

### Frontend (take-test.blade.php)

**Таймер (якщо є обмеження часу):**
```blade
@if($test->duration_minutes)
    <div class="ml-6">
        <div class="bg-blue-50 border-2 border-blue-200 rounded-lg p-4">
            <div class="flex items-center justify-center mb-2">
                <svg>...</svg>
                <span>Залишилось часу</span>
            </div>
            <div 
                x-data="timer(@js($test->duration_minutes * 60), @this)"
                x-init="startTimer()"
                :class="колір залежно від часу">
                <span x-text="formatTime()">00:00</span>
            </div>
        </div>
    </div>
@endif
```

**JavaScript (Alpine.js):**
```javascript
Alpine.data('timer', (initialTime, component) => ({
    timeRemaining: initialTime,
    interval: null,
    
    startTimer() {
        this.interval = setInterval(() => {
            this.timeRemaining--;
            
            if (this.timeRemaining <= 0) {
                clearInterval(this.interval);
                // Автоматично завершуємо тест
                component.call('autoSubmitTest');
            }
        }, 1000);
    },
    
    formatTime() {
        const minutes = Math.floor(this.timeRemaining / 60);
        const seconds = this.timeRemaining % 60;
        return `${String(minutes).padStart(2, '0')}:${String(seconds).padStart(2, '0')}`;
    }
}));
```

**Логіка:**
1. Створюється інтервал який кожну секунду зменшує `timeRemaining`
2. Коли `timeRemaining <= 0` → викликається `autoSubmitTest()`
3. Функція `formatTime()` конвертує секунди в формат `ХХ:ХХ`
4. Клас динамічно змінюється залежно від часу (синій/помаранчевий/червоний)

---

## Сценарії використання

### Сценарій 1: Екзамен з обмеженням часу

**Налаштування:**
```
Назва: Іспит з історії України
Тривалість: 60 хвилин
Кількість питань: 30
Кількість спроб: 1
```

**Процес:**
1. Студент розпочинає тест
2. Таймер показує `60:00`
3. Студент відповідає на питання
4. Таймер тікає: `59:59`, `59:58`, ...
5. Коли `00:00` → тест автоматично завершується
6. Результат зберігається (навіть якщо не всі питання мають відповіді)

### Сценарій 2: Швидкий тест з обмеженням

**Налаштування:**
```
Назва: Швидкий тест на знання термінів
Тривалість: 5 хвилин
Кількість питань: 10
Кількість спроб: 3
```

**Процес:**
1. Студент має 5 хвилин
2. Таймер показує `05:00`
3. При `01:00` таймер стає помаранчевим (попередження)
4. При `00:30` таймер стає червоним (критично)
5. При `00:00` → автоматичне завершення

### Сценарій 3: Тест без обмеження часу

**Налаштування:**
```
Назва: Тест на розуміння матеріалу
Тривалість: (порожнє) - без обмеження
Кількість питань: 20
Кількість спроб: необмежено
```

**Процес:**
1. Студент розпочинає тест
2. Таймера немає
3. Студент може витратити стільки часу, скільки потрібно
4. Завершує тест вручну кнопкою "Завершити тест"

### Сценарій 4: Користувач не встиг

**Ситуація:**
```
Тест: 20 питань, 30 хвилин
Користувач відповів на: 15/20 питань
Не відповів на: 5 питань
Час вийшов на 29:59
```

**Результат:**
```
Автоматичне завершення
Правильних відповідей: 12/15 (з тих на які відповів)
5 питань без відповіді = 0 балів
Загальний результат: 12/20 = 60%
```

---

## Переваги системи

### ✅ Для адміністратора

1. **Гнучкість**
   - Можна встановити ліміт або залишити без обмеження
   - Різна тривалість для різних тестів

2. **Контроль**
   - Гарантує, що тест буде пройдено за певний час
   - Запобігає зловживанням (списуванню з інтернету)

3. **Справедливість**
   - Всі студенти мають однакові умови
   - Автоматичне завершення без втручання

### ✅ Для користувача

1. **Прозорість**
   - Бачить скільки часу залишилось
   - Візуальні підказки (кольори)

2. **Попередження**
   - Помаранчевий колір за 5 хвилин
   - Червоний колір за 1 хвилину
   - Можна планувати час

3. **Захист**
   - Не втратить прогрес при закінченні часу
   - Результат збережеться автоматично

---

## Приклади налаштувань

### Короткий тест (5-15 хвилин)

```
Назва: Швидка перевірка знань
Тривалість: 10 хвилин
Питань: 10
Спроб: 2
Призначення: Низький ризик

Ідеально для: експрес-опитувань, перевірки термінології
```

### Середній тест (30-60 хвилин)

```
Назва: Контрольна робота
Тривалість: 45 хвилин
Питань: 25
Спроб: 1
Призначення: Високий + Низький ризик

Ідеально для: контрольних робіт, модульних тестів
```

### Екзамен (60+ хвилин)

```
Назва: Іспит з предмету
Тривалість: 90 хвилин
Питань: 50
Спроб: 1
Призначення: Всі користувачі

Ідеально для: фінальних іспитів, атестацій
```

### Практичний тест (без обмеження)

```
Назва: Практичне завдання
Тривалість: (без обмеження)
Питань: 15
Спроб: необмежено
Призначення: Низький ризик

Ідеально для: домашніх завдань, тренувальних тестів
```

---

## Візуальні приклади

### Таймер - різні стани

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│ 🕐 Залишилось  │     │ 🕐 Залишилось  │     │ 🕐 Залишилось  │
│   часу        │     │   часу        │     │   часу        │
│   30:00       │     │   04:30       │     │   00:45       │
│   (синій)     │     │ (помаранчевий)│     │  (червоний)   │
└─────────────────┘     └─────────────────┘     └─────────────────┘
   Багато часу            Поспішати!           Критично!
```

### Екран завершення тесту

```
┌──────────────────────────────────────────┐
│              ✅                           │
│                                          │
│       Тест завершено!                    │
│                                          │
│  Ваші результати успішно збережено       │
│                                          │
│  [Повернутися до тестів]                 │
│  [Переглянути мої результати]            │
└──────────────────────────────────────────┘
```

---

## Технічні деталі

### Таймер - як працює

1. **Ініціалізація**
   ```php
   $this->timeRemaining = $test->duration_minutes * 60; // 30 хв = 1800 сек
   ```

2. **Відлік**
   ```javascript
   setInterval(() => {
       this.timeRemaining--; // Кожну секунду -1
   }, 1000);
   ```

3. **Перевірка**
   ```javascript
   if (this.timeRemaining <= 0) {
       component.call('autoSubmitTest'); // Викликаємо Livewire метод
   }
   ```

4. **Форматування**
   ```javascript
   formatTime() {
       minutes = floor(1800 / 60) = 30
       seconds = 1800 % 60 = 0
       return "30:00"
   }
   ```

### Підрахунок без відповіді

```php
foreach ($this->questions as $question) {
    $givenAnswer = $this->answers[$question['question_id']] ?? '';
    
    // Порожня відповідь = неправильно
    if (empty($givenAnswer)) {
        $isCorrect = false;
    } else {
        $isCorrect = $givenAnswer === $question['correct_answer'];
    }
}
```

### JSON результату

**З відповіддю:**
```json
{
  "question_id": 1,
  "given_answer": "B",
  "is_correct": true
}
```

**Без відповіді:**
```json
{
  "question_id": 2,
  "given_answer": null,
  "is_correct": false
}
```

---

## Змінені файли

### Backend

✅ **database/migrations/2026_02_16_190414_add_duration_to_tests_table.php**
- Додано поле `duration_minutes`

✅ **app/Models/Test.php**
- Додано `duration_minutes` до fillable
- Додано cast до integer

✅ **app/Livewire/Tests/CreateTest.php**
- Властивість `duration_minutes`
- Валідація
- Збереження

✅ **app/Livewire/Tests/EditTest.php**
- Властивість `duration_minutes`
- Завантаження з БД
- Оновлення

✅ **app/Livewire/UserTests/TakeTest.php**
- Властивості `startTime`, `timeRemaining`
- Метод `autoSubmitTest()`
- Рефакторинг `submitTest()` → `completeTest()`
- Логіка для питань без відповіді

### Frontend

✅ **resources/views/livewire/tests/create-test.blade.php**
- Поле "Тривалість тесту (хвилин)"
- Placeholder та підказки

✅ **resources/views/livewire/tests/edit-test.blade.php**
- Поле "Тривалість тесту (хвилин)"
- Збереження значення

✅ **resources/views/livewire/user-tests/take-test.blade.php**
- Блок таймера в header
- Кольорова індикація (синій/помаранчевий/червоний)
- JavaScript для таймера (Alpine.js)

---

## Статус

✅ **Міграція виконана**
✅ **Модель оновлена**
✅ **Форми створення/редагування оновлені**
✅ **Таймер реалізований**
✅ **Автоматичне завершення працює**
✅ **Підрахунок балів коректний**
✅ **Код валідний**
✅ **Готово до використання**

---

**Дата створення:** 16.02.2026  
**Тип зміни:** Feature (Нова функціональність)  
**Пріоритет:** High  
**Версія:** 1.0

🕐 **Система таймера для тестів повністю функціональна!**

